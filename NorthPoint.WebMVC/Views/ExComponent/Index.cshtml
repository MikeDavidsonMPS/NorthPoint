@model IEnumerable<NorthPoint.Models.ExComponent.ExComponentListItem>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.PropertyId)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.ExterId)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.PropertyId)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.ExterId)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ExterId }) |
            @Html.ActionLink("Details", "Details", new { id = item.ExterId }) |
            @Html.ActionLink("Delete", "Delete", new { id = item.ExterId })
        </td>
    </tr>
}

</table>
